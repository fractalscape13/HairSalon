@{
  Layout = "_Layout";
}

@model HairSalon.Models.Client;

<h2>Are you sure you want to delete @Html.DisplayFor(model => model.Name)?</h2>

<div class="box">
  @using (Html.BeginForm())
  {
      <input class="btn" type="submit" value="Delete"/>
  }
  @Html.ActionLink("Back to all clients", "Index")<br>
  @Html.ActionLink("Back to all stylists", "Index", "Stylists")<br>
  @Html.ActionLink("Back to homepage", "Index", "Home")
</div>